// main page view which contains
// 1) a search component (with an input form, a + button and a submit button)
// 2) a saved recipes component (with a saved recipes list and a saved recipe list item component)
// 3) a recipe list component (with a recipe list item component)